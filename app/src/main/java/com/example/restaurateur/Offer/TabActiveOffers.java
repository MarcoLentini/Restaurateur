package com.example.restaurateur.Offer;import android.os.Bundle;import android.support.v4.app.Fragment;import android.support.v4.app.FragmentTransaction;import android.support.v7.widget.RecyclerView;import android.view.LayoutInflater;import android.view.View;import android.view.ViewGroup;import com.example.restaurateur.R;import com.example.restaurateur.Reservations;public class TabActiveOffers extends Fragment {    private RecyclerView recyclerView;    private RecyclerView.LayoutManager layoutManager;    private RecyclerView.Adapter categoriesAdapter;    private Reservations reservationsActivity = (Reservations) getActivity();    @Override    public View onCreateView(LayoutInflater inflater, ViewGroup container,                             Bundle savedInstanceState) {        // Inflate the layout for this fragment        View view= inflater.inflate(R.layout.fragment_tab_aoffers, container, false);        loadFragment(new TabCategoryActiveOffers(),"active");        return view;    }    private void loadFragment(android.support.v4.app.Fragment fragment,String type) {        // load fragment        FragmentTransaction transaction = getActivity().getSupportFragmentManager().beginTransaction();        Bundle bundle = new Bundle();        bundle.putString("Type", type);        // set Fragmentclass Arguments        fragment.setArguments(bundle);        transaction.replace(R.id.frame_container_active_offers, fragment);        transaction.commit();    }}